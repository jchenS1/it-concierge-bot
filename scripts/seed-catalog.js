#!/usr/bin/env node
/**
 * seed-catalog.js
 * Fetches entitlements from ConductorOne and writes them to catalog/entitlements.json.
 *
 * TODO: implement (post-MVP, corresponds to WF-4)
 *   1. Load C1 credentials from .env
 *   2. Fetch OAuth2 token
 *   3. GET /api/v1/apps/{C1_APP_ID}/entitlements
 *   4. Map to catalog schema (app_id, entitlement_id, app_name, label, aliases, ...)
 *   5. Write to catalog/entitlements.json
 */

console.log('TODO: not yet implemented');
